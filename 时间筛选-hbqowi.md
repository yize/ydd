# 时间筛选

## 1. 简介​

时间筛选可以实现【单值】、【区间】的设置

## 2. 操作步骤​

### 2.1 选择组件，配置数据集​

选择数据集
### 2.2 选择字段​

从右边字段选择对应的日期类型字段到【查询字段】内，如图：
**注意：查询字段类型必须为日期类型**

添加查询字段
### 2.3 设置默认值​

固定值下可以设置是固定的日期还是变量，如图：

设置默认值
**注：也可以设置为区间，需要先在样式设置中开启区间配置**

样式设置区间
有区间的限制就会分为两种情况：**第一种未开启区间设置默认值，如图：**

设置默认值固定值

设置默认值变量**第二种开启区间设置默认值，如图：**

设置默认值固定值

设置默认值变量
### 2.4 设置样式​

样式设置
**必填**：开启后该日期筛选项就要必填
**区间**：开启区间后，日期可以选择开始日期与结束日期
**标题**【显示标题】：开关，默认打开，如关闭下方标题设置均同步隐藏【标题名称】：该筛选项的标题名称 【标题提示】：鼠标移到标题上会有个小叹号，起到提示的效果【标题位置】：上方、左侧、内部，默认上方

组件标题
**内容：**输入框提示：输入框内的提示

**整体样式**：【风格】：有普通、隐藏、禁用、只读【最大宽度】：默认 446，
为提升用户体验会根据页面大小对筛选器宽度自适应，但自适应时如超过最大宽度则不会继续加宽【整体尺寸】：默认中，有小、中、大可设置【清除按钮】：可以设置是否一键清除，在输入框后面会有个 X 按钮

## 3. 扩展阅读​

报表筛选组件怎么筛选时间区间？

--------------------欢迎关注我们--------------------